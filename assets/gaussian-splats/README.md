# 高斯泼溅资产目录

此目录用于存放校园扫描或渲染生成的 `.splat` / `.ksplat` 资产。

- `manifest.json` 是唯一坐标来源，单位统一为米，Y 轴向上。
- 高斯泼溅只负责高细节视觉；墙体、地面、楼梯、门和交互仍使用 Three.js 低模代理碰撞。
- 每个资产必须按区域裁切，禁止把整个校园打成一个超大文件。
- 原点必须放在分区中心地面，导出前冻结旋转和缩放。
- 文件未准备好时保持 `enabled: false`，当前游戏不会受影响。

建议单区桌面端控制在 100–150 万 splats，移动端另做约 30% 密度版本。
